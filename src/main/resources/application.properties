server.port=8080

spring.application.name=brewery-monolith

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.write-date-timestamps-as-nanoseconds=true

spring.messages.basename=messages/messages

# Propiedades para customizar el nombre y la contraseña del usuario por defecto de Spring Security
# Por defecto, el nombre es "user" y la contraseña se genera automáticamente cada vez que arranque la aplicación(se muestra en la consola)
#spring.security.user.name=spring
#spring.security.user.password=admin

#logging.level.com.mlorenzo.brewery=debug
#logging.level.org.springframework.security=debug
